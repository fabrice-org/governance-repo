# Demo Ruleset Baseline

Scope: repositories in `fabrice-org` demo set.

Ruleset intent:
- enforce PR-only changes to `main`
- require Code Owners and required status checks
- enforce review gate consistency across domain repos

This file is documentation for the org settings configuration used in the live demo.
